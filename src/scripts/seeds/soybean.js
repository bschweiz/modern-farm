 //make and export function createSoybean etc//
 export const createSoybean = () => {
    let soybeanObj = {
        type: "Soybean",
        height: 20,
        output: 4,
    }
    return soybeanObj
}